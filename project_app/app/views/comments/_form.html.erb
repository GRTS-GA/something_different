
<%= form_for (comment)  do | f |%>
        <%=f.hidden_field :event_id%>

        <%= f.text_area :content, placeholder: "Click to add comment", class: "commentBox", require: true%>
        <br>

        <%= f.submit "Submit" %>

<% end %>

